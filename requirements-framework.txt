# Dependencies of the BRT orchestration process itself.
# Project-under-test dependencies are created per benchmark instance by iCoRe.
datasets==5.0.0
packaging==26.0
requests==2.34.2
tomli>=2.0; python_version < "3.11"
